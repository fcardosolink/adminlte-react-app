import React from 'react'

export const LabelSucess = (props) => (
    <span className="label label-success">{props.title}</span>
)

export const LabelWarning = (props) => (
    <span className="label label-warning">{props.title}</span>
)

export const LabelInfo = (props) => (
    <span className="label label-info">{props.title}</span>
)

export const LabelDanger = (props) => (
    <span className="label label-danger">{props.title}</span>
)

